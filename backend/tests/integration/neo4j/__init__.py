"""
Neo4j integration tests package.

This package contains integration tests for Neo4j graph database operations,
including entity sync, relationship sync, tenant isolation, and error handling.

Tests require a running Neo4j instance and will be skipped if unavailable.
"""
